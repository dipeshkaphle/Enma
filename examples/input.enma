let z = "abc";

data Position = {
	x: int;
	y: int;
}

let x : int = 10;

fn is_origin(pos: Position ){
	let x = true;
	if (pos.x == 0 and pos.y == 0 ){
		x= true;
	}else{
		x= !true;
	}
	x;
}

// This is error
//$$

fn sum(n: int ): int  {
	let i : int = 0;
	let sm : int = 0;
	while( i <= n){
		sm = sm + i;
		i =  i + 1;
	}
	sm;
}

